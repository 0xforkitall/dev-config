{
  "name": "@0xforkitall/dev-config",
  "description": "Shared config for development",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "^18.12.0",
    "npm": "please-use-yarn"
  },
  "scripts": {
    "versionup": "lerna version --conventional-commits --create-release github --yes --no-push",
    "release": "lerna publish from-package --yes"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.0.2"
  }
}
